函数
	1、和java中的方法是类似的
	2、也是一个可以被重复利用的代码片段，完成一种特定功能的代码块
	3、实现的语句
		第一种方式
		function 函数名 (){}
		第二种方式
		var = fuction（）{}
	4、调用方式
		函数名字(参数)
	5、假如传递的参数超过之前定义的形式参数，这时候只会传进去指定的个数的参数
	6、不存在函数的重写
	7、假如函数的名字有一样的时候，根据代码执行至上而下的原理得知，之前编写的函数会被之后的函数覆盖掉