tomcat
	文件目录的分层
	1、webapps是服务器响应文件的地方，也就是想要浏览得到什么就放着里面
	2、work 就是工作空间，存放的是实例化对象执行里面方法的地方
	3、lib就是服务器能完美运行所需要外部jar包
	第一次的运行
		纯代码形式的
			再webapps文件下创建自己的一个文件夹存放自己的图片什么的然后cmd在bin目录下面开始startup启动tomcat
			服务器，然后到浏览器去输入 http://localhost:8080/自己文件夹/自己文件格式